# v0.5.0 D4 Manual Regression Script

## Goal

Provide a minimal, executable UI regression runbook for release-blocking flows on the primary environment.

## Execution Context

- Date: 2026-04-04
- Primary host: macOS 26.3.1 (Apple Silicon)
- App build: `bcabd27` (`release/v0.5.0`)

## Preconditions

0. (Optional but recommended) prepare manual asset bundle:
   - `./scripts/v050_prepare_manual_ui_assets.sh --label <runner-label>`
   - Use generated `plain.txt` and `manual-report-template.md`.
1. Build and launch app from Xcode (`Moaiy` scheme, Debug or Release).
2. Ensure at least one test recipient key can be generated/imported.
3. Prepare local files:
   - `plain.txt` with sample content (`hello moaiy`)
   - an exported armored public key file (`pub.asc`)
   - an exported armored secret key file (`sec.asc`) for restore-like import checks
4. Ensure write permission to a temp directory (for encrypt/decrypt output files).

## Test Cases

| ID | Flow | Steps | Expected Result | Status | Notes |
|---|---|---|---|---|---|
| MR-01 | Key generation | `Create Key` -> enter valid name/email/type -> confirm | Key created, appears in list after refresh, metadata renders | Pass (Manual) | Reported in `dist/manual-ui/v0.5.0-manual-user-manual-20260404-150210/manual-report.md` |
| MR-02 | Key import (public) | `Import Key` -> select `pub.asc` -> import | Success banner; imported count increments | Pass (Manual) | Reported in `dist/manual-ui/v0.5.0-manual-user-manual-20260404-150210/manual-report.md` |
| MR-03 | Key import (secret) | `Import Key` -> select `sec.asc` -> import | Secret key imported; private-key capable actions available | Pass (Manual) | Initial fail fixed; re-test passed on repackaged DMG (`dist/Moaiy-0.5.0-debug-20260404-155038.dmg`) |
| MR-04 | Key export (public) | On a key card -> action menu -> `Export Public Key` | File saved; armored content begins with PGP public header | Pass (Manual) | Reported in `dist/manual-ui/v0.5.0-manual-user-manual-20260404-150210/manual-report.md` |
| MR-05 | Key export (secret) | On secret key card -> action menu -> `Export Private Key` -> passphrase | File saved; armored content begins with PGP private header | Pass (Manual) | Reported in `dist/manual-ui/v0.5.0-manual-user-manual-20260404-150210/manual-report.md` |
| MR-06 | Text encrypt/decrypt happy path | Encrypt test text to recipient; decrypt with corresponding secret key/passphrase | Decrypted text equals original input | Done (Automated) | `TextEncryptionFlowTests/textEncryptDecrypt_roundtrip` passed |
| MR-07 | Text decrypt error path | Decrypt with wrong passphrase or non-matching secret key | User-facing error shown; no crash | Done (Automated) | `TextEncryptionFlowTests/textDecrypt_invalidPassphrase_fails` + `textEncrypt_invalidRecipient_fails` passed |
| MR-08 | File encrypt/decrypt happy path | Drop `plain.txt` on key card -> encrypt output; then decrypt output file | Decrypted file content equals original | Pass (Manual) | Reported in `dist/manual-ui/v0.5.0-manual-user-manual-20260404-150210/manual-report.md` |
| MR-09 | File conflict handling | Encrypt/decrypt to existing output filename | Non-conflicting filename created or overwrite handling behaves as designed | Done (Automated) | `KeyActionFilePlannerTests/nonConflictingURL_appendsNumericSuffix` + `FileEncryptionFlowTests/fileEncryptDecrypt_roundtrip_andConflictHandling` passed |
| MR-10 | Trust and signing | Open key actions -> trust management update; detached sign + verify | Trust update persisted; signature verification reports valid | Done (Automated) | `TrustManagementTests` + signature parsing tests passed |
| MR-11 | Backup/restore entry availability | Inspect user-visible entry points from key action menu | Backup/Restore entry is visible and can open backup panel | Done (Automated) | `KeyActionMenuAvailabilityTests/backupMenuVisible_forPublicKey` + `backupMenuVisible_forSecretKey` passed; panel-open click-through remains optional sanity check |

## Automated Evidence (Kickoff)

- Command:
  - `xcodebuild test -project /Users/codingchef/Taugast/moaiy/Moaiy/Moaiy.xcodeproj -scheme MoaiyTests -destination 'platform=macOS' -derivedDataPath /tmp/moaiy-deriveddata -only-testing:MoaiyTests/KeyGenerationTests -only-testing:MoaiyTests/KeyImportExportTests -only-testing:MoaiyTests/KeyManagementIntegrationTests -only-testing:MoaiyTests/TrustManagementTests -only-testing:MoaiyTests/GPGServiceTests -only-testing:MoaiyTests/KeyActionFilePlannerTests -only-testing:MoaiyTests/KeyActionBatchResultPlannerTests -only-testing:MoaiyTests/BackupExportSummaryTests`
- Result:
  - `** TEST SUCCEEDED **` (83 passed / 0 failed)
- Scope:
  - Confirms core logic for key generation/import-export/trust/signature parsing/file-output planning.
  - Remaining D4 closure still requires UI click-through confirmation for key create/import/export + file flow path and full manual checklist sign-off.

## Automated Evidence (Text Flow)

- Command:
  - `xcodebuild test -project /Users/codingchef/Taugast/moaiy/Moaiy/Moaiy.xcodeproj -scheme MoaiyTests -destination 'platform=macOS' -derivedDataPath /tmp/moaiy-deriveddata -only-testing:MoaiyTests/TextEncryptionFlowTests`
- Result:
  - `** TEST SUCCEEDED **` (3 passed / 0 failed)
- Covered cases:
  - text encrypt/decrypt roundtrip success
  - decrypt failure with wrong passphrase
  - encrypt failure with invalid recipient

## Automated Evidence (File Flow)

- Command:
  - `xcodebuild test -project /Users/codingchef/Taugast/moaiy/Moaiy/Moaiy.xcodeproj -scheme MoaiyTests -destination 'platform=macOS' -derivedDataPath /tmp/moaiy-deriveddata -only-testing:MoaiyTests/FileEncryptionFlowTests`
- Result:
  - `** TEST SUCCEEDED **` (1 passed / 0 failed)
- Covered cases:
  - file encrypt/decrypt roundtrip
  - destination conflict handling with numeric suffix for encrypt/decrypt outputs

## Recorder Template

Use this section while executing:

- Runner:
- Start time:
- End time:
- Build hash:
- Pass count:
- Fail count:
- Blocked count:
- Filed issues:

## Manual Execution Evidence (2026-04-04)

- Report:
  - `dist/manual-ui/v0.5.0-manual-user-manual-20260404-150210/manual-report.md`
- Summary:
  - Initial run passed: `MR-01`, `MR-02`, `MR-04`, `MR-05`, `MR-08`
  - Initial run failed: `MR-03` (`key import failed`)
  - Re-test update (2026-04-04): `MR-03` passed after import hotfix and repackaging
- Additional UI notes from report:
  - success prompt content alignment issue on key creation
  - bottom logo/text overlay obscures part of key card list

## Completion Criteria (D4 Manual)

- Manual-required set (`MR-01` to `MR-05`, `MR-08`) is executed with explicit Pass/Fail.
- Automated-only set (`MR-06`, `MR-07`, `MR-09`, `MR-10`, `MR-11`) remains green in latest smoke evidence.
- Any Fail/Blocked item has issue link and severity.
- `P0/P1` unresolved count is `0` before release tag.

## Moaiy {{TAG}}

Build Time (UTC): {{BUILT_AT_UTC}}
Target Commit: `{{TARGET_COMMIT}}`
Gate Mode: `{{GATE_MODE}}`
Signing Mode: `{{SIGNING_MODE}}`

### English

This release is generated by the protected Moaiy release workflow (v2), with version sync checks, architecture-specific packaging, and auditable metadata.

#### Highlights

- Strict release preflight enforces `origin/main` alignment and `build-and-test` success.
- Dual-architecture DMG artifacts are packaged and checksummed in one run.
- Release metadata is captured in `release-manifest.json` and `SHA256SUMS.txt`.

#### Assets

{{ASSET_TABLE}}

#### Known Issues

{{KNOWN_ISSUES}}

### 中文（简体）

该版本通过 Moaiy 受保护的发布工作流（v2）生成，包含版本一致性校验、双架构打包和可审计元数据。

#### 关键变更

- 发布前强制校验 `origin/main` 与 `build-and-test` 状态。
- 一次流程产出双架构 DMG 并生成校验值。
- 发布元数据统一记录在 `release-manifest.json` 与 `SHA256SUMS.txt`。

#### 资源文件

{{ASSET_TABLE}}

#### 已知事项

{{KNOWN_ISSUES}}
